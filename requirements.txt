# Dependency specification for "Generative Synthetic Data for Causal Inference"
# Captured from the conda env used for all experiments (Python 3.9.23).
# Reproduce with:  conda create -n llm_aug python=3.9 && pip install -r requirements.txt
#
# GPU note: torch is pinned to a CUDA 12.1 build (torch==2.5.1+cu121). On a CPU-only
# or different-CUDA machine, install a matching torch build instead (e.g. `pip install torch==2.5.1`).

numpy==1.26.3
pandas==2.3.2
scipy==1.13.1
scikit-learn==1.6.1
statsmodels==0.14.6
matplotlib==3.9.4

# Tabular generators
be-great==0.0.9          # GReaT LLM tabular generator (serializes rows as text, fine-tunes GPT-2)
ctgan==0.12.1            # CTGAN conditional-GAN tabular generator
rdt==1.21.0              # CTGAN dependency (reversible data transforms)

# Deep-learning / LLM backend for GReaT
torch==2.5.1+cu121
transformers==4.56.1
tokenizers==0.22.0
accelerate==1.10.1
datasets==4.1.0
